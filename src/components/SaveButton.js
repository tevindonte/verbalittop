import React from 'react';

const SaveButton = ({ images }) => {
  const handleSave = () => {
    // Save the canvas as a high-resolution image
  };

  return <button onClick={handleSave}>Save Canvas</button>;
};

export default SaveButton;
